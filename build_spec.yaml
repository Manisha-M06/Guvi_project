version: 0.1

timeoutInSeconds: 600

shell: bash

phases:
  install:
    commands:
      - echo "Installing dependencies (if any)"

  build:
    commands:
      - echo "Build started"
      - ls -la

  post_build:
    commands:
      - echo "Build completed"

artifacts:
  files:
    - '**/*'
